import styled from "styled-components";
import { StyledBar } from "../../shared";

export const StyledDaysBar = styled.div`
  display: flex;
  width: 100%;
  height: 4rem;
  overflow-x: hidden;
  position: relative;
`;
export const StyledDaysWrapper = styled(StyledBar)`
  justify-content: space-between;
`;
