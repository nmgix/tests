import { DiscountHeader } from "src/entities/discount";
import "./header.scss";

export const Header = () => {
  return (
    <header className='page-header'>
      <DiscountHeader />
    </header>
  );
};
